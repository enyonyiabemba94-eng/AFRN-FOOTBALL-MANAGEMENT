(()=>{
'use strict';
if(!/player-profile\.html$/i.test(location.pathname)||window.__AFRN_PLAYER_RECORD__)return;window.__AFRN_PLAYER_RECORD__=true;
const db=window.supabaseClient;if(!db)return;const id=new URLSearchParams(location.search).get('id');if(!id)return;
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
async function load(){try{const [{data:p,error:pe},{data:ev,error:ee},{data:ms,error:me}]=await Promise.all([db.from('players').select('id,registration_approval_status,registration_requested_at,registration_approved_at,status,club_id').eq('id',id).maybeSingle(),db.from('match_events').select('id,match_id,event_type,minute').eq('player_id',id),db.from('matches').select('id,status').limit(1000)]);if(pe)throw pe;if(ee)throw ee;if(me)throw me;const final=new Set((ms||[]).filter(m=>['FINISHED','COMPLETED','FULL TIME','FT'].includes(String(m.status||'').toUpperCase())).map(m=>String(m.id)));const e=(ev||[]).filter(x=>final.has(String(x.match_id)));const goals=e.filter(x=>/goal|goli|penalty|own goal/i.test(String(x.event_type||''))).length;const yellow=e.filter(x=>/yellow|njano/i.test(String(x.event_type||''))).length;const red=e.filter(x=>/red|nyekundu/i.test(String(x.event_type||''))).length;const matches=new Set(e.map(x=>x.match_id)).size;let box=document.getElementById('afrn-official-record');if(!box){box=document.createElement('section');box.id='afrn-official-record';const app=document.getElementById('app');if(app)app.appendChild(box)}box.innerHTML=`<div class="afr"><div class="afr-head"><div><b>🏛️ AFRN OFFICIAL PLAYER RECORD</b><small>Rekodi ya Shirikisho</small></div><span>${esc(String(p?.registration_approval_status||p?.status||'—'))}</span></div><div class="afr-grid"><div><b>${matches}</b><small>Mechi</small></div><div><b>${goals}</b><small>Mabao</small></div><div><b>${yellow}</b><small>🟨 Njano</small></div><div><b>${red}</b><small>🟥 Nyekundu</small></div></div><div class="afr-meta">Usajili: ${esc(p?.registration_approval_status||'—')} · Tarehe ya ombi: ${esc(p?.registration_requested_at?new Date(p.registration_requested_at).toLocaleDateString('sw-TZ'):'—')} · Uidhinishaji: ${esc(p?.registration_approved_at?new Date(p.registration_approved_at).toLocaleDateString('sw-TZ'):'—')}</div></div>`}catch(e){console.warn('AFRN official player record',e)}}
const s=document.createElement('style');s.textContent=`#afrn-official-record{margin-top:12px}.afr{background:#fff;border:1px solid #d9e1ec;border-radius:16px;padding:14px;box-shadow:0 3px 12px #14213d10}.afr-head{display:flex;justify-content:space-between;align-items:start}.afr-head b{color:#071a33}.afr-head small{display:block;color:#667085;font-size:10px;margin-top:3px}.afr-head span{font-size:9px;font-weight:900;padding:5px 8px;border-radius:20px;background:#eef4ff;color:#174ea6}.afr-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin-top:11px}.afr-grid div{background:#f7f9fc;border-radius:10px;text-align:center;padding:9px}.afr-grid b{display:block;font-size:19px;color:#071a33}.afr-grid small{font-size:9px;color:#667085}.afr-meta{font-size:9px;color:#667085;margin-top:10px;line-height:1.5}@media(max-width:520px){.afr-grid{grid-template-columns:repeat(2,1fr)}}`;document.head.appendChild(s);setTimeout(load,600);setInterval(load,15000);
})();